<Directory "{protected_directory_path}">
	AuthType Basic
	AuthName "Authentication Required"
	AuthUserFile "{protected_directory_credentials_file}"
	Require valid-user
	Order allow,deny
	Allow from all
</Directory>
